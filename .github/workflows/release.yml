name: "tagged-release"

on: 
  push:
    tags:
    - "release-*"
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
  release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: ${{ needs.build.outputs.sdistname }}
      - run: |
          pwd
          ls -l
      - uses: ncipollo/release-action@v1
        with:
          artifacts: ${{ needs.build.outputs.sdistname }}
